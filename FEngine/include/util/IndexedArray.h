#include <vector>

typedef int * array_id;

template<typename Type>
class IndexedArray
{
public:
	array_id const Push(Type object)
	{

	}

	void Remove(array_id id)
	{

	}

	void Get(array_id id)

private:
	std::vector<Type * > m_data;

};